<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arif_ayon.checkmark">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_activity" />
        <activity android:name=".Home_Activity" />
        <activity android:name=".Create_New_Account_Activity" />
        <activity android:name=".CGPA_Calculator_Activity" />
        <activity android:name=".Database.Database_Handler" />
        <activity android:name=".Model.Contact" />
        <activity android:name=".Event_Form_Activity" />
        <activity android:name=".Menu_Activity" />
        <activity android:name=".Routine_Activity"></activity>
    </application>

</manifest>